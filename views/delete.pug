extends layout

block content

    body
        h1= title
        p= `Are you sure you want to delete ${book.title}?`
        form(method="POST")
            p
                input(type="submit" value="Yes")
            p
                a.button(href=`/books/${book.id}`) Nevermind